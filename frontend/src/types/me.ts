/** Espelha backend/app/api/schemas/me.py::CurrentUserRead. */
export interface CurrentUser {
  id: string;
  institution_id: string;
  external_subject: string;
  full_name: string;
  role: string;
}
